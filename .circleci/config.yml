version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:11.0.3-jdk
    steps:
      - checkout
      - run: javac Athena.java
      - run: jar cfe Athena.jar Main Athena.class
      - run: git add Athena.jar
      - run: git push
